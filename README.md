# bootstrap4-scrollspy-horizontal
Modified Bootstrap scrollspy.js v4.4.1 (https://getbootstrap.com/) to support horizontal scroll

Basically, all the `top` references were replaced by `left` and `height` by `width`.

